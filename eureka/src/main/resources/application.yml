server:
  port: 8761

eureka:
  instance:
    hostname: 120.24.227.178
  client:
    registerWithEureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}

spring:
  application:
    name: eureka-server